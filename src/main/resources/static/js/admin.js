if(!Review.admin){
    Review.admin = {};
}

document.addEventListener("DOMContentLoaded", function () {
    if (!Review) throw new Error("Review object not found");



    /*
    const $tpl = $('.review--moderate-buttons-template').clone();
    $("body").append($tpl);
    */
});